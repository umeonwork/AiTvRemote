# Web client

This is a minimal Vite + React client for AiTvRemote.

Run locally:

1. cd client
2. npm install
3. npm run dev

Set the API endpoint by creating a .env file with VITE_API=http://localhost:3001 if needed.
